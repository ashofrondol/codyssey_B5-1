# 12일 학습 순서표

> 하루에 실행 과제는 딱 하나. 그 이상 하면 다음 날 안 하게 된다.

**지금 할 일 — 하나만**

지금 A4 백지 한 장에 customer·category·menu·order_header·order_detail 다섯 테이블과 FK 화살표, 그리고 각 테이블의 행 수(10/10/12/12/20)를 손으로 그려라.

---

## 학습 순서표 — 12일

전제: 이 학습자는 개념을 모르지 않는다. **입이 늦고, 근거 숫자가 없고, 원리 한 칸 아래를 못 판다.** 따라서 이 표는 읽는 시간을 최소로 잡고 **말하기와 손 훈련**에 시간을 몰아 준다. 하루에 실행 과제는 **딱 1개**다. 여러 개 하지 마라.

### 기본 규칙 세 줄

1. 모든 말하기 훈련은 **휴대폰 녹음을 켜고** 한다. 안 들으면 안 는다.
2. 모든 손 훈련은 **`fresh.db`(01+02만 적용)** 에서 한다. 저장소의 `cafe.db`는 Q13·Q14·Q15가 이미 반영된 사후 상태라 교재 숫자와 안 맞는다.
3. 하루 30~60분. 그 이상 하면 다음 날 안 한다.

```bash
# 매번 이걸로 초기 상태 DB를 만든다
cd /home/coder/volume/codyssey_B5-1
python3 - <<'PY'
import sqlite3, os
os.path.exists("fresh.db") and os.remove("fresh.db")
c = sqlite3.connect("fresh.db")
for f in ("01_schema.sql", "02_data.sql"):
    c.executescript(open(f, encoding="utf-8").read())
c.commit()
print(c.execute("SELECT COUNT(*) FROM order_header").fetchone())  # 12 나오면 정상
PY
```

---

### 1주차 — 입을 뚫는다 (읽기 속도와 근거 숫자)

| 일 | 읽을 모듈 | 오늘의 손 훈련 **1개** | 통과 기준 |
|---|---|---|---|
| **D1** | `reading-drill` §1 (실행 순서 표) 만 | **스키마 카드를 손으로 그린다.** A4 백지에 5개 테이블·컬럼·FK 화살표·CASCADE 위치·행 수(cust 10 / cat 10 / menu 12 / oh 12 / od 20)를 적는다. 다 그린 뒤 백지에 한 번 더. | 두 번째 장을 **90초 안에** 완성 |
| **D2** | `reading-drill` §1 다시 (표만 소리 내어) | **Q6·Q7·Q8을 녹음하며 읽는다.** 다섯 문장 틀(①그레인 ②조인과 행 수 ③WHERE ④묶기 ⑤결과 행 수와 이유)을 채워 말한다. 대상은 `drill/03_queries_naked.sql`. | 3개 모두 TTF **10초 이내** |
| **D3** | `join` §1 (곱하고 나서 거른다) | **행 수를 직접 세어 본다.** `SELECT COUNT(*) FROM customer, order_header`(120) → ON 붙이면 12. 이어서 Q8을 INNER→LEFT로 바꿔 9행/10행을 눈으로 확인. | 굿즈가 사라졌다 살아나는 걸 **손으로 재현** |
| **D4** | `join` 조인 방향 표(N→1 / 1→N) | **Q10을 세 번 변형해 돌린다.** ① 원본(ON절 필터) ② `status` 필터를 WHERE로 이동 ③ `COALESCE` 제거. 각각 행 수와 값을 적는다. | 10행 / 7행 / NULL 등장을 **표로 정리** |
| **D5** | `subquery` §0~§1 (3단계 상자 읽기) | **04_bonus (1-a)(1-b)(1-c)를 녹음하며 읽는다.** \"이 상자는 ___를 돌려줍니다\"를 반드시 먼저. 그다음 셋 다 `EXPLAIN QUERY PLAN`을 붙여 계획 차이를 종이에 옮겨 적는다. | JOIN 쪽에만 `USE TEMP B-TREE FOR DISTINCT`가 있는 걸 **스스로 지적** |
| **D6** | 없음 (읽지 않는 날) | **1회전 전체 드릴.** Q6·Q7·Q8·Q10·Q11-B·Q12 여섯 개를 연속으로 녹음. 각 쿼리 TTF와 총 소요를 스톱워치로 적는다. | 6개 평균 TTF **10초 이내**, 총 소요 90초 이내 |
| **D7** | 녹음 되듣기 | **루브릭 채점.** 6개 녹음을 들으며 5칸 루브릭(그레인/행 수/실측 숫자/대안 대조/말끝)을 채운다. 3칸 이하인 쿼리에 표시. | 표시된 쿼리 목록이 2주차 재훈련 대상 |

---

### 2주차 — 한 칸 아래를 판다 (원리와 결함 지적)

| 일 | 읽을 모듈 | 오늘의 손 훈련 **1개** | 통과 기준 |
|---|---|---|---|
| **D8** | `constraint` 전체 | **제약을 네 번 깨뜨린다.** `fresh.db` 사본에서 FK/UNIQUE/CHECK/NOT NULL을 각각 위반시키고, 에러 메시지 원문과 `e.sqlite_errorname`을 표로 받아 적는다. 이어서 `CHECK (a>=0)` 테이블에 NULL을 넣어 **통과하는 것**을 확인. | \"CHECK는 NULL을 못 막는다\"를 **실측으로 증명** |
| **D9** | `index` §0~§2 (어원, 언어별 인덱싱) | **없는 인덱스를 찾아낸다.** `EXPLAIN QUERY PLAN`으로 `WHERE menu_id=1`, `WHERE category_id=1`, `DELETE FROM category WHERE id=9`가 SCAN인 걸 확인 → 인덱스 2개를 만들고 계획이 바뀌는 걸 재확인. | 부모 삭제가 `SCAN menu`에서 `SEARCH`로 바뀌는 걸 **캡처** |
| **D10** | `index` B-Tree·복합 인덱스 절 | **인덱스의 대가를 잰다.** 10만 행짜리 사본을 만들어 인덱스 유무로 INSERT 시간과 파일 크기를 잰다. 숫자 두 쌍만 얻으면 끝. | \"쓰기 2.4배 느려지고 파일 27.7% 커진다\"를 **내 숫자로** |
| **D11** | `normalization` §1~§2, §8 | **결함을 재현하고 막는다.** `order_detail`에 `(order_id=1, menu_id=1)`을 한 줄 더 INSERT → 성공하는 걸 확인 → `UNIQUE(order_id, menu_id)`를 걸고 다시 시도해 막히는 걸 확인. | 같은 주문에 같은 메뉴 2줄이 들어가는 걸 **직접 봄** |
| **D12** | `normalization` 브릿지 절 | **N:M을 하나 새로 설계한다.** `option` + `menu_option(extra_price)`을 백지에 쓰고 실제로 CREATE. 설계 판단 4개(복합 PK 이유 / extra_price 위치 / CASCADE vs RESTRICT / option_id 인덱스)를 녹음하며 설명. | 4개 판단을 **막힘 없이** 설명 |

---

### 3주차 — 밖으로 넓힌다 + 리허설

| 일 | 읽을 모듈 | 오늘의 손 훈련 **1개** | 통과 기준 |
|---|---|---|---|
| **D13** | `db-selection` §1 (트랜잭션·ACID) | **`last_insert_rowid()` 함정을 재현한다.** 헤더 1건 + 상세 2줄을 전부 `last_insert_rowid()`로 넣어 FK 에러를 받아 본다. 그다음 `cur.lastrowid`를 변수로 받아 고친다. | FK를 끄면 **조용히 고아 행이 들어가는 것**까지 확인 |
| **D14** | `db-selection` 상황별 선택 절 | **기능 5개 배정표를 백지에 쓴다.** 주문 결제 / 대기번호 / 매출 리포트 / 메뉴 검색 / 세션에 DB를 배정하고 이유를 한 문장씩. 다 쓴 뒤 녹음하며 말한다. | 이유가 전부 **접근 패턴**으로 서술됨 |
| **D15** | 꼬리질문 20개 | **꼬리질문 답변 녹음.** 20개를 무작위 순서로 뽑아 한 문장씩 답한다. 모르는 건 \"확답 못 드리겠습니다, 확인 방법은 ___입니다\"로. | 20개 중 **16개 이상 3초 내 답** |
| **D16** | 없음 | **2회전 전체 드릴.** D6과 같은 6개 + D7에서 표시한 재훈련 쿼리. | TTF **5초 이내**, 총 소요 60초 이내 |
| **D17** | 없음 | **역방향 드릴.** 스키마 카드만 보고 B1~B5를 백지에 쓴 뒤 `fresh.db`에서 돌려 행 수 검증. | 5개 중 4개가 **첫 시도에 정답 행 수** |
| **D18** | 없음 | **모의 평가 1회.** 다른 사람(또는 카메라)에게 15분간 발표: 스키마 설계 3분 → Q6/Q10 낭독 4분 → 인덱스 3분 → 정규화·N:M 3분 → 결함 자진 신고 2분. | 침묵 3초 이상 **0회** |

---

### 마지막 날 (평가 전날)

읽지 마라. 스키마 카드를 백지에 한 번 그리고, 3회전 드릴(TTF 3초 / 총 40초)만 돌린다. 새로운 걸 넣으면 오히려 말이 엉킨다.

### 이 표가 노리는 것

| 피드백 | 어느 날에 갚는가 |
|---|---|
| 2·6·8 (속도·주석·자신감) | D2, D5, D6, D7, D16, D17, D18 — **7일치**. 가장 많이 배분했다 |
| 5 (인덱싱 원리) | D9, D10 |
| 3 (CHECK 실동작) | D8 |
| 7 (정규화·N:M) | D11, D12 |
| 1·4 (DB 사용법·선택) | D13, D14 |

읽기는 총 9일, 손과 입은 총 18일치다. **비율이 뒤집혀 있으면 이 표를 잘못 쓰고 있는 것이다.**
